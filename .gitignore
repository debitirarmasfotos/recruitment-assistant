# Secrets - never commit
.env
.env.*
!.env.example

# Python
.venv/
venv/
__pycache__/
*.py[cod]

# Runtime logs (created by the app at runtime; never commit)
logs/

# AAMAD local config (keep the example template, ignore local state)
aamad.config.yml

# OS / editor
.DS_Store
Thumbs.db
